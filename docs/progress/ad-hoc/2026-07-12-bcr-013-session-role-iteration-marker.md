# BCR-013 评估方案：工作流会话「角色+迭代」结构化标识

- 日期：2026-07-12
- 模式：Workflow Audit / BCR 评估
- 执行身份：参谋长（agent-workflow 第二工作目录）
- 关联：coordination `REQUESTS.md` BCR-013（提报方 workboard · PM，2026-07-12）
- 状态：**Owner 采信基线、拍板分裂（2026-07-12，部分采纳）**——角色维交解析侧（不进框架）；迭代维进框架（参谋长建议方案 C，待与 workboard 共定 token 契约后落地）。本文含评估 + 迭代维落地方向。

> 本文是评估游标；BCR 状态真源仍以 coordination `REQUESTS.md` 为准。参谋不决策：本文只出方案与建议，采纳与否在 Owner。
> **修订说明（2026-07-12）**：初版曾把方案 B 定性为「确定性 1.0」、把方案 C「明确不采纳」、把 D 贬为「辅」，经 Owner review 核出硬伤，本版已纠正（见「评估」「选项」「建议」）。

## 决策记录（2026-07-12 · Owner 拍板，采纳参谋长建议）

- **前置门（决策点 1）= 先走第 0 档探底**：零框架改动，先让 workboard 解析侧挖存量、量化缺口，再定是否进框架。
- **token 契约落点（决策点 5）= coordination `contracts/`**：将来若定 token 格式落 contracts/（跨项目契约正规位），须满足抗误命中 + 可正则。
- **决策点 2/3/4 暂挂**：B 定性、排序、C 复活——待第 0 档两维数据回来、且只对判定进框架的那一维再拍。
- **第 0 档执行归属 = workboard 解析侧迭代**（非框架、不占本 BCR 落地）；交付规格见下「建议·第 0 档」+「第 0 档收敛判据」。参谋长待其数据回来复评。

## 第 0 档基线摸底（参谋长只读调研，2026-07-12）

> SSH 直连服务器，复用真实 `detectRole` + 复刻 `userContents` 抽取口径，跑 `/root/.claude/projects/` 全部 **59 个**有用户内容的会话。只读、未改任何东西。**这是参谋长摸底、非正式第 0 档交付**（正式那步含"扫 assistant 消息改进 + 真命中率"，仍归 workboard）。

**角色维（现成 detectRole，改进前）：**

- 头条 Unknown 率 **27.1%**（16/59）；"你是XX"精确命中 44.1%，加关键词兜底整体识别 72.9%。
- 拆项目看，**真实开发项目近满分**：xiaobao 1/25、workboard 0/5、ai 1/6、agent-workflow 1/4（合计约 3/40 ≈ **7.5%**）；16 个 Unknown **绝大多数落在 coordination(3/3)、root、参谋长/General 这类本就非开发角色的会话**——它们 Unknown 反而正确。
- **口径修正**：Unknown 率门槛应在"工作流开发角色会话"上量（排除 General/coordination/参谋长），否则 27% 头条把非开发会话算进来、误导门槛判定。按开发会话看，角色维**改进前就已 ~7.5%、近或已达 ≤10% 门槛**。

**迭代维（`vX.Y` 信号可得率）：**

- 首条用户消息含 1.7%（1/59）、前 5 条用户消息 6.8%、全文任意处 66.1%（与预研 71% 一致，验证了方法）。
- **迭代号在会话源头近乎为零**，全文有但定位不到——解析侧造不出源头没有的锚点。（量的是信号可得率，非真命中率。）

**结论**：基线**强烈印证两维分裂**——角色维存量充足、改进前已近够用（交解析侧、大概率不进框架）；迭代维源头信号近零（需框架补迭代锚点，且只承载迭代、不碰角色，改动最小）。方案结构（第 0 档→两维分判→D主C验B增）经数据验证成立，无需改结构；仅按上「口径修正」收紧门槛度量口径。

## 迭代维落地方向（2026-07-12 · Owner 采信基线、采纳分裂）

**采纳记录（2026-07-12，Owner 拍板）**：

- **角色维**：交 workboard 解析侧、不进框架、跳过正式第 0 档。**并定：`detectRole` 兜底 `Unknown → General`（没切工作流角色即 General——本就是框架默认语义）→ 角色维完全解析侧闭合、零框架、无 Unknown 孤儿**（基线那 16 个"Unknown"全部正确归 General）。
- **迭代维**：进框架；契约落点 = **框架 baseline**（参谋长可写；黑名单不许碰 coordination `contracts/`，故不走 contracts/）；方案 C。**但见下「参谋长补充」——付框架税前建议再探一个解析侧选项。**

**D 还是 C —— 窄化到"仅迭代维 session 绑定"后，参谋长改荐 C（不是总排序里的 D）：**

- 迭代维的真问题是"把**一个会话**绑到它所属的迭代"，且要覆盖**进行中**会话（workboard 最想实时看的）。
- **D（收尾迭代记录带标识）**：只在收尾落、只覆盖已关闭迭代，且迭代记录记的是"这个迭代发生了什么"、不天然记录 session 身份 → 对 session 绑定不直接、且漏掉进行中。
- **C（启动写映射行）**：会话启动时写一行 `项目 + 角色 + 迭代（取自 INDEX「当前迭代」）+ 时间戳`，workboard 按（项目, 时间窗, 角色）绑定 → **覆盖进行中 + 已关闭，直接解决 session↔迭代绑定**。并发同角色撞窗风险在单 Owner 薄公司极低。
- **为何与总排序"D 主力"不同**：总排序是"承载 role+iteration 通用 + 服务审计"场景下 D 最原生；此处已窄化为"仅迭代维、需覆盖进行中、要 session 级绑定"，C 更贴合。D 可留作后续审计增强（迭代记录补迭代摘要），不作本次主力。

**token 契约草案（映射行格式）**：字段 = 项目 / 角色 / 迭代 / 时间戳；格式须 (a) 独占行强定界、可确定性正则（如追加写一份 `.jsonl` 每行一条，或 `<!-- wf-iter: project=… role=… iter=… ts=… -->`）；(b) 迭代取自 `INDEX.md`「当前迭代」，无则记 `无`。

**闸（不先落框架）**：token 契约是跨项目契约，须与 workboard **共同定稿并登记 coordination `contracts/`** 后，才改框架（`runtime.md`/入口「工作流启动」加"启动写映射行"约定）+ 回流下游。契约未定，框架侧不先动。

**待 Owner**：批 C 方向 + 授权参谋长牵头定 baseline 里的映射行格式。

## 参谋长补充（迭代维付框架税前，2 点 · 2026-07-12）

speccing 方案 C 时撞到两点，值得在改框架、回流 4 个下游之前先过一下：

**1. C 要真"确定"，得靠 hook，不能靠 LLM 启动时自己写。**
C 的卖点是"落盘=确定"。但若让**助手在工作流启动时写映射行**，它是否每次都写、格式对不对又回到 LLM 不确定——和方案 B 同一个病。C 要兑现确定性，须由 **SessionStart hook**（框架随附 hook 配置）确定性写，带 hook 落地成本 + 每会话永久税。

**2. 迭代维可能根本不用进框架——一个没考虑到的解析侧信号源：**
基线量的是"会话内容里有没有 `vX.Y`"（近零）。但迭代归属还有**另一路信号**：**会话时间戳 × 项目 `INDEX.md` 的 git 历史**——某会话在时间 T 属于哪个迭代 = 那时该项目 `INDEX.md`「当前迭代」的值，git 历史里查得到。**免疫"会话不提 vX.Y"、零框架、零永久税**；workboard（服务器侧能读各仓 `.git`）即可重建 session↔迭代。

**建议**：付框架税前，先像第 0 档那样**只读探一把 INDEX-git-history 解析侧路**，量它能把多少会话归到具体迭代。达标 → 迭代维也交解析侧、**整条 BCR-013 零框架**；不达标 → 再上框架 C（且用 hook）。与本 BCR 一路"能解析侧就别加框架税"的取向一致。

**待 Owner**：迭代维——先只读探 INDEX-git-history 解析侧（参谋长可代跑，荐），还是直接上框架 C（hook）？

## 背景（问题属实，已实证）

下游 workboard 要把「会话 ↔ 角色 ↔ 迭代」精准对应，目前只能靠启发式猜。核对消费方源码 `niuma-cheng-workboard/src/server/sync/session-meta.js`：

- **角色识别** `detectRole()`：① 首条用户消息命中 `你是XX` → 置信度 1.0；② 否则扫前 5 条用户消息做关键词加权 → 0.5~0.9；③ 打平不硬猜 → `Unknown`。
- **迭代识别**：**该文件完全没有迭代识别逻辑**。BCR 附 workboard Developer 预研：`vX.Y` 在会话开头仅 3%、标题 0 次、全文 71% 但无法定位到具体迭代。

**已实证的数据管道前提**（`claude-sync.js`）：`288-290` 行 user 与 assistant 消息**都已 parse 入库**（存进 `messages`）；但 `299-307` 行 `userContents` 只收 user 消息前 5 条，`detectRole` 只喂它——**assistant 消息存了却没参与识别**。含义：workboard 若要多用一个信号源，**改的只是 `detectRole` 的输入，数据管道不用动**。

结论：角色信号仅当用户恰好打了「你是XX」才可靠，迭代维度几乎无信号，**准确度天花板卡在会话源头**——但下游解析侧仍有一块**未挖的存量**（已入库的 assistant 消息）。

## 评估

**1. 归口正确。** target = agent-workflow，属框架层议题，落职责③（BCR-009 起参谋长兼评估落地）。

**2. 「不为单一下游耦合」这条张力——比初版更严格地看：** 一条机器可读的「本会话=什么角色·哪个迭代」标识，只有当它**落进框架自己的产物（迭代记录）**时，才真正服务框架内生需求（流程审计直读落盘产物，不读会话转录）。**读会话转录里 assistant 播报那一行的，目前只有 workboard。** 所以：

- **落盘形态（写进迭代记录 / 映射真源）= 框架原生**，审计直接受益，自证成立。
- **assistant 播报一行 = 主要仍是喂 workboard 的实时可见性**，自证偏弱——这点初版高估了，本版纠正。

**3. 设计岔路——marker 落点，四选项（按「越靠落盘越确定、越服务审计」排序）：**

| 方案 | 形态 | 确定性 | 服务对象 | 代价/风险 |
|------|------|--------|----------|-----------|
| **C 落盘「时间戳+角色+迭代」映射真源** | 启动/收尾机制在 `docs/progress/` 写映射行（提报方明细候选②的改良：不依赖 session id，用时间戳） | **字段确定**；绑定：有 session id 则确定，走(项目,时间窗,角色)匹配则**同角色并发会话有撞窗歧义** | 框架 + 审计原生 | 需验证：会话能否在启动时可靠写盘；并发撞窗如何消歧 |
| **D 收尾把角色+迭代写进迭代记录** | `mechanisms.md` 收尾机制写入（落盘产物） | **确定**（用户确认收尾时写） | 框架 + 审计原生 | **只覆盖走到收尾的会话**；中途放弃/进行中会话无记录；滞后 |
| **B assistant 启动输出固定格式标识行** | 「工作流启动」后助手输出一行（角色+迭代，迭代自 `INDEX.md` 派生） | **非确定**——LLM 每轮是否精确吐固定格式不保证（会漏/变体/被上文带偏）；**是「高置信度启发式」，强于现状，但不是 1.0**。要真确定需 hook/模板强制输出（另一份成本） | 主要喂 workboard 实时可见性 | 下游只需改 `detectRole` 输入源（管道不动）；但信号本身不硬 |
| A 用户手打结构化触发 | 要求用户开场打 `角色=… 迭代=…`（明细候选① = A 的改良：复用「你是XX」+补迭代号） | 确定（用户输入） | workboard | **违背自然语言入口**、迭代号仍要用户手打易漏——不建议 |

**关键纠正**：初版把 B 说成「确定性 1.0」是错的。现有代码给 1.0 是因为信号源是**用户确定输入**（「你是XX」）；B 把信号源换成 **LLM 自述**，模型输出本身不确定。真确定的信号只在「用户输入」或「落盘文件」，不在「模型自述」。

## 一个未点破的共性局限（四方案都受制）

「一会话 = 一角色一迭代」是**静态快照假设**，与真实会话动态不符：

- 一个会话可能先关 `v0.2` 再启 `v0.3`（跨迭代）；
- 参谋长 / General 会话不属任何迭代；
- 迭代可能在会话中途推进。

B、D 实际是「两端采样」（启动播报一次 / 收尾落一次），**中间段变更是盲区**。C 若在每次角色/迭代切换时补写映射行可缓解，但也更重。**任何方案都不是全覆盖**——Owner 决策时须知此非「一次标识解决一切」。

## 建议（经 review 修订，待 Owner 拍板）

**不再是「直接采纳 B」。按成本从低到高分档，建议先探底再决定是否动框架：**

**第 0 档（推荐先做，零框架改动）**：让 workboard 在**解析侧**先挖存量——`detectRole` 扫已入库的 assistant 消息、改进启发式，实测缺口到底多大。这正是「框架改动不为单一下游耦合」推到底的结论：**缺口未证实材质之前，不给框架加永久约定**。这一档不需要本 BCR 落地框架，归 workboard 迭代。

**第 0 档收敛判据（两维各设门槛、各自判定，不用复合"且"）**：角色维与迭代维的信号存量天差地别——角色维有已入库 assistant 存量可挖（第 0 档正是挖它），迭代维预研显示 `vX.Y` 全文命中 71% 却无法定位、现状命中 ≈ 0 且自然语言缺可靠迭代锚点。合成一个"且"门槛会让**注定不达标的迭代维拖累其实够用的角色维**，把角色维一起拽进框架税。故拆两维、各判各的：

- **角色维**：报 `detectRole` 的 **Unknown 率**（识别不出角色的会话占比）——**须在工作流开发角色会话上量，排除 General/coordination/参谋长这类本就应 Unknown 的会话**（参谋长基线摸底：头条 27.1% 含非开发会话，按开发会话看仅 ~7.5%）。门槛（数值待 workboard + Owner 定，建议初值 Unknown ≤ 10%）达标 = 解析侧够用、**角色维不进框架**；不达标才进。
- **迭代维**：报 **迭代命中率**（正确归属到具体迭代的会话占比，现状 ≈ 0）。门槛（建议初值 ≥ 80%）达标 = 解析侧够用；不达标 = **迭代维进框架**（预研预示纯解析侧大概率到顶、注定进）。

**含义（把框架改动压更小）**：真实结论很可能是分裂的——角色维交解析侧、迭代维进框架。那样框架 marker **只需承载迭代锚点、不必碰角色**，D/C 只为迭代维做，永久税进一步缩小；决策点 1 也不再是"整体二选一"，而是两维各自判进/不进。

**若第 0 档证明缺口仍显著，再进框架改动，且排序应是：**

1. **D 为主力**（落盘、确定、审计原生）——框架本该做：收尾把角色+迭代写进迭代记录。
2. **C 待验证候选**——若「启动可靠写盘 + 并发消歧」验证通过，C 是唯一「字段确定 + 结构化 + 审计原生」形态，可升为主。
3. **B 为增量**（且如实标注为「高置信度启发式，非 1.0」）——只在需要**进行中会话实时可见性**时加，或上 hook/模板强制才谈「确定」。

**token 契约（无论 B/C/D 哪条落地都要先定）**——格式必须满足两条硬约束，写进契约要求：

- (a) **抗误命中**：标识独占一行、强定界（如 `<!-- wf-meta: role=… iter=… project=… -->`），不能被正常散文撞上；
- (b) **确定性可正则**：字段与格式固定、可稳定正则解析。

契约落点建议 coordination `contracts/`（跨项目契约正规位），框架侧不先于契约落地。

## 待 Owner 决策点（分层：先拍前置门，再定其余）

**前置门（先拍这一个）：**

1. **是否先走第 0 档探底**（角色维、迭代维都先解析侧探底，零框架改动，押后永久流程税）？探底**不出整体结论，而是按两维各自门槛出两个独立判定**（见上「第 0 档收敛判据」）：角色维够不够、迭代维够不够，很可能一个够一个不够。

**依赖决策点 1 的结果（只对"判定进框架"的那一维才需拍；先探底则暂挂待数据）：**

2. **B 的定性**：若某维用 B，接受降级为「高置信度启发式」，还是上 hook 求真确定（加成本）？
3. **排序**：进框架的维用 D 主力（落盘/审计原生）> C 待验证 > B 增量？——**注意若只迭代维进框架，marker 只需承载迭代锚点、不碰角色，改动更小**。
4. **C 是否复活**：验证「启动写映射行 + 时间窗匹配 + 并发消歧」后再定？

**可与决策点 1 并行（不依赖探底）：**

5. **token 契约落点**：coordination `contracts/` 还是 BCR 明细？

## 提报方诉求归属（BCR 关闭时须说清，避免落空）

BCR-013 提报方（workboard PM）的原始诉求是**实时**精准匹配「会话↔角色↔迭代」。须注意归属：

- **框架 D（收尾落盘）服务的是审计留痕，天然不覆盖进行中会话**——恰是 workboard 最想实时看的那段。
- 若最终走「D 主力 + 第 0 档」，则**正面满足提报方实时诉求的是第 0 档（解析侧启发式），不是框架 D**。
- 这不是错，但 **BCR 关闭时必须写清「谁满足了 workboard 的实时诉求」**，别让提报方以为框架 D 覆盖了实时段、结果发现没覆盖。

## 前置/遗留

- **提报方明细已于本评估同期补齐**：coordination `REQUESTS.md` `## BCR-013` 明细段已写全（含候选①②），已随 BCR 推进一并提交（coordination `5b33afa`）；本文已回读对齐（候选① = A 的改良，候选② = C 的改良）。
- **本版为经 Owner review 修订的初评**：硬伤（B 定性 1.0、C 枪毙、D 贬辅）已纠正；采纳方向与档位仍待 Owner 拍板。

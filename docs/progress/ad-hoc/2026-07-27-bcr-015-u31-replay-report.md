# BCR-015 U3.1 · 门禁真回放定性归档（R3-G3 完成条件）

> 参谋长会话，2026-07-27。`L1_REPLAY=1 sh ci/gates/l1-gates.sh <仓>` 对三下游全部存量迭代记录只读回放（忽略时代标记），报错逐条人工定性。回放退出码 1（有发现）、正常模式退出码 0（历史被时代标记正确隔离）均验证。

## 定性结论：解析逻辑验证通过——发现 40+ 条，全部为「真问题（历史遗留，不回改）」，**解析误报 0**

| 仓 / 记录 | 发现 | 定性 |
|---|---|---|
| workboard v0.1 | G1×2（部署状态「已部署」「部署通过」） | 真·历史遗留（BCR-011 前旧词表；现行封闭枚举为 待检查/检查中/通过/阻塞/已跳过） |
| workboard v0.3（进行中） | G1×11（含验收类自造状态词）+ **G4×3（R4 收尾轮 / R5 / R6）** | 真·进行中迭代沿用旧格式（无时代标记，不执法）。**值得注意**：轮次命名已飘出 R1-R3 纪律（把收尾/反馈修复当轮次记）——其下一个迭代（L1 时代）起，Review 轮次须回到 R1→R3→阻塞，收尾/验收反馈类动作应记入「阶段执行记录」而非 Review 轮次 |
| xiaobao v0.1/v0.2/v0.5/v0.6 | G1 多条（已完成 / R1 完成等待汇总 / Review中（待…）等长文状态）+ G4（v0.1 R4） | 真·历史遗留（BCR-011 提报动机正是这批记录；不回改） |
| ai v0.1/v0.2 | G1 多条（同类旧词表） | 真·历史遗留；v0.2 为进行中旧格式记录，不执法 |
| 全仓 G2 / G5 / Change Note | 零发现 | 旧记录无新表结构（G2 被自身标记正确门控）；无打回残留 |

**方法论备注**：回放的目的是验证解析器在真实数据上的行为（R3-G3——「fixture 集与实现同构」的补偿），不是审判历史。每条发现均已人工对回原文确认「文件里确实写着这个非标值」，故判为解析正确命中而非误报。BCR-011「历史不回改」约束执法不回溯，本回放为只读验证，不冲突。

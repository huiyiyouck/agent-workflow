# BCR-003 + BCR-005 合并定稿落地 spec（生态参与者系统化）

- 日期：2026-06-25
- 范围：**BCR-005（父·生态参与者拓扑+跨界协议）+ BCR-003（子·元信息同步台账+根 `/root/Project/CLAUDE.md` 重设计）**。**不含 BCR-004（删 UI 角色，另算）**。
- 设计方：agent-workflow 真源会话（General）
- 状态：**合并 spec，待 reviewer review → 定稿 → 落地**
- 关联：coordination `REQUESTS.md` BCR-003（评估中）/ BCR-005（评估中）
- 来源游标：`2026-06-25-bcr-005-ecosystem-participant-topology.md`、`2026-06-25-root-claude-md-redesign.md`（本 spec 是这两份的**落地整合定稿版**；细节推理见来源，本文只承载"最终改什么、改哪、什么顺序"）

> **本文是评估/落地附件，非状态真源。** BCR-003/005 流转状态以 coordination `REQUESTS.md` 为准。

---

## 〇、reviewer 用：一句话看懂

把生态里的**非开发参与者**（协调台账 coordination、生态索引根 `/root/Project/CLAUDE.md`、框架真源自己）系统定义进工作流（**BCR-005**），并在此拓扑下补一条**项目元信息同步机制**（**BCR-003**），消除"元信息悄悄过期"和"Owner 当人肉转述"两个缺口。下面是要落地的**全部精确改动 + 顺序**。

## 一、落地改动总览（按文件 / 权限节点）

| # | 文件 | 节点 | 谁落地 | 改什么 | 来源 |
|---|------|------|--------|--------|------|
| 1 | `docs/baseline/cross-project-collaboration.md` | 框架真源（我可直写） | agent-workflow 真源会话 | 新增两节：`## 生态参与者与跨界协议`（总）+ `## 项目元信息同步`（分） | 005 + 003 |
| 2 | `docs/baseline/mechanisms.md` | 框架真源（我可直写） | agent-workflow 真源会话 | 迭代关闭检查新增第 9 项 | 003 |
| 3 | coordination `STATUS.md` | 协调台账（我可直写） | 真源会话直写 or coordination 会话 | 新增 `## 元信息变更台账` 区块 | 003/005 |
| 4 | coordination `PROJECTS.md` | 协调台账 | coordination 会话 | 订正 workboard 行（照实况）；BCR-001 回流清单 workboard 状态 | 003 |
| 5 | `/root/Project/CLAUDE.md` | 生态索引根（仅根会话） | 根目录会话（读 `PROJECTS.md`） | 套重设计结构 + 照清单填值 | 003/005 |

> 顺序依赖（Finding 2）：**改动 1 必须先于/同步改动 2**（第 9 项引用「§项目元信息同步」节）。改动 5 必须在改动 4 之后（根索引照已订正的 `PROJECTS.md`）。

## 二、改动 1 · `cross-project-collaboration.md` 新增两节（精确文案）

- 插入位置：`## 基线修正提案流转（BCR）` 之后、`## communications（按需求）` 之前。
- 节序：先 005 总则节，后 003 数据流节；003 节引用 005 矩阵（Finding 1 已协调，无重复）。

````markdown
## 生态参与者与跨界协议

生态参与者分四类，各有固定性质、边界与跨仓写权限。**非开发参与者（协调台账、生态索引）也是受工作流管控的一等参与者，不游离**。

| 节点 | 性质 | 装 dev 工作流? | 边界与职责 | 跨仓写权限 |
|------|------|----------------|------------|------------|
| 框架真源 | 工作流产品 single source，自我演进 | 否（General 直接维护制品，不走角色/Review 门禁） | 维护 baseline/模板/入口；评估·采纳·落地 BCR | 直写协调台账 BCR 池；不改下游业务代码；不改生态索引文件 |
| 协调真源台账 | 跨项目边界事实 single source（契约/REQ/BCR/状态/项目目录），纯 Markdown | 否（门禁空转，General 直接维护台账） | 登记·回填 REQ/BCR/元信息；维护状态·项目目录·契约；是 BCR 登记处、非回流对象 | 维护自己台账；不直写其他仓 |
| 开发型下游 | 业务开发项目，接入 dev 工作流 | 是 | 仓内按角色/门禁开发 | 向协调台账登记 REQ/BCR/元信息台账行；不改 baseline、不改别的下游 |
| 生态索引根 | 生态导航视图（如生态根导航文件），受工作流管控 | 否（只读导航 + 索引维护职责） | 照项目目录真源同步索引；订正本文件不算改子项目 | 对协调台账只读：输出回执/待写内容，不直写 |

跨界写协议：

1. **授权直写优先**：节点对某事有权威**且** [P0] 允许写目标仓时，直接跨仓写（先满足 [P0]：确认仓位置 + git 同步 + 改动范围 + 只写跨项目事实），**不经 Owner 转述**。
2. **转交仅限跨权限边界**：仅当本节点对目标仓**无权限**时才转交。
3. **转交走「读文件 / 精确制品」**，**禁止靠 Owner 口头/retype 重述状态真源**（有损信道）。

「谁能写什么」矩阵：

| 目标 ＼ 写方 | 框架真源 | 协调台账 | 开发型下游 | 生态索引根 |
|-------------|---------|----------|-----------|-----------|
| `baseline/` | ✅ | ❌ | ❌（只提 BCR） | ❌ |
| 协调台账 | ✅（BCR 池） | ✅（自有） | ✅（REQ/BCR/元信息） | ❌（只读+回执） |
| 生态索引文件 | ❌ | ❌ | ❌ | ✅ |
| 下游业务仓 | ❌ | ❌ | ✅（自己） | ❌ |

## 项目元信息同步

项目迭代中变更**定位/名称/技术栈/上线状态/工作流接入状态**时，须保证生态层真源被同步订正，避免悄悄过期。本机制是上节参与者间的一条具体跨界数据流，写权限按上节矩阵。

- **真源**：项目元信息结构化单一真源是协调台账项目目录（`PROJECTS.md`）；生态索引是其导航视图，照真源同步、不自由发挥。
- **载体**：变更登记在协调台账状态文件（`STATUS.md`）「元信息变更台账」——字段 `项目 | 字段 | old | new | 来源（commit/迭代） | PROJECTS 已同步 | 生态索引已同步`。
- **三方接力**：
  1. 子项目迭代关闭检查发现变更 → 登记一行（两「已同步」列留空）；
  2. 协调台账会话照 new 值改 `PROJECTS.md` → 勾「PROJECTS 已同步」；
  3. 生态索引维护方照已更新的 `PROJECTS.md` 订正导航视图 → 通知协调台账会话勾「生态索引已同步」（索引维护方对协调台账只读时，输出回执转交）。
- **真源可疑时停机**：索引维护方发现各仓实况与 `PROJECTS.md` 不一致且台账无在途行时，**不得**按实况直接改索引、**不得**同步到过期真源，须输出待登记台账行转交协调台账会话先订正 `PROJECTS.md`，再同步。
````

> 通用性把关：以上**通用措辞**，不在 baseline 硬编码 `/root/Project/CLAUDE.md`；本生态把「生态索引根」实例化为根 `CLAUDE.md` 的职责，写在根文件自己（改动 5）。

## 三、改动 2 · `mechanisms.md` 第 9 项（精确文案）

- 插入位置：§3 迭代关闭检查 → `### 检查项` 列表末尾，第 8 项之后。

```markdown
9. 本迭代是否变更了项目定位 / 名称 / 技术栈 / 上线状态 / 工作流接入状态？若是，由关闭检查执行者在协调台账 `STATUS.md`「元信息变更台账」登记一行（项目 / 字段 / old / new / 来源），后续由协调台账会话据此同步 `PROJECTS.md`、生态索引维护方照真源订正其导航视图（详见 `cross-project-collaboration.md` §项目元信息同步）。
```

## 四、改动 3 · coordination `STATUS.md` 新增台账区块

新增区块（命名一致性 Finding 3：**本生态台账列名钉死用「生态索引已同步」**，与 baseline 通用措辞一致）：

```markdown
## 元信息变更台账

> 用途见各项目 `cross-project-collaboration.md` §项目元信息同步。两「已同步」列都勾后该行归档/移除。

| 项目 | 字段 | old | new | 来源（commit/迭代） | PROJECTS 已同步 | 生态索引已同步 |
|------|------|-----|-----|----------------------|-----------------|----------------|
```

## 五、改动 4 · coordination `PROJECTS.md` 存量订正

- **workboard 行**：照实况订正（定位「项目管理工作台/统一管理中枢」、技术栈「React + shadcn」、状态「已上线 `workboard.huiyiyou.cloud`」、接入「已接入」）——**值由 coordination 会话照 workboard 实况核对后填**（本 spec 不代填，避免真源臆测）。
- BCR-001 回流清单 workboard 状态由「不适用（未接入）」订正为实际接入状态。

## 六、改动 5 · 根 `/root/Project/CLAUDE.md` 重设计（根会话落地）

结构与落地核对清单**完全沿用** `2026-06-25-root-claude-md-redesign.md` §四（结构骨架）+ §五（数据值核对清单），不在此重复。要点：

- 新增「本文件的定位（受工作流管控）」+「索引维护职责」两节；
- 职责边界补「订正本文件不算改子项目」例外；
- 索引表表头说明：除「分类（内部）」外照 `PROJECTS.md` 同步；
- 数据值照**已订正的** `PROJECTS.md` 填（依赖改动 4 先完成）。
- 「生态索引根」节点定义来自改动 1 的 BCR-005 节，本文件是其本生态实例化。

## 七、落地顺序（跨权限节点）

```text
1. 框架真源（真源会话直写）：改动 1（cross-project 两节）→ 改动 2（mechanics 第9项）
   └ 自检：./scripts/measure-context.sh（固定层增量）+ scripts/sync-downstream.sh（会回流 baseline，必跑）
2. 协调台账（真源会话按 [P0] 直写 or coordination 会话）：改动 3（建台账区块）→ 改动 4（订 PROJECTS.md workboard + BCR-001 清单）
3. 生态索引根（根目录会话，读 PROJECTS.md，不 retype）：改动 5（根 CLAUDE.md 重设计 + 填值）
4. BCR 状态推进（协调台账）：BCR-003/005 → 已采纳 → 已落地真源 → 回流中
5. 下游 sync 回流：ai / xiaobao / workboard 跑 sync-downstream → coordination 置「已回流下游」终态
```

## 八、给 reviewer 的检查点

1. §二两节的**节序与不重复**（005 总则 / 003 数据流，003 引用 005 矩阵）是否协调好？
2. §二**四类参与者**划分、边界、跨仓写权限矩阵是否准确（尤其协调台账「非回流对象」、生态索引根「只读+回执」）？
3. **跨界写协议三条**是否真能消除"人肉转述"（回归用例：BCR-003 评估记录 garble `748dc22`→`58de4eb`）？
4. **通用 baseline 不硬编码根路径**的取舍是否保持？
5. **台账列名**通用层「生态索引已同步」/ 本生态层「根索引已同步」两层是否接受（§四已钉死本生态用前者）？
6. **落地顺序**的两处依赖（改动1→2、改动4→5）是否正确？

## 九、未决 / 边界

- **BCR-004（删 UI 角色）不在本 spec**，连评估方案都还没写，排在本 spec 落地之后单独处理。
- 改动 4/5 的**数据值**由 coordination/根 会话照实况与 `PROJECTS.md` 核对填，本 spec 不代填（防真源臆测，遵守停机/转交协议）。
- 验证：本 spec 仅文档/状态核对，未跑脚本；脚本自检在落地步骤里执行。

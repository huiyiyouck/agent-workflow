# 全自动迭代改造整体方案 v1（L2 · 整迭代自动化）

> 发起：指挥官（2026-08-08 会话）；代拟：参谋长。
> 状态：**v1 待指挥官正式 review**（2026-08-08 指挥官明确指出「方案我还没有 review」，此前会话答复仅为对 §8 拍板点的**初步意向**，以正式 review 结论为准）。
>
> **会话初答记录（2026-08-08，供 review 时参照，非定稿依据）**：
> 1. A4 修订案：「按照你推荐的来」（意向认可可逆性切分）。
> 2. 中断条件：「5 个中断我也认可」（预算阈值未另定，默认值待 review 确认）。
> 3. L3 挂起：「先单个项目做全自动，多个项目打合、沟通确认等我有项目实例过后再来做开发」（意向认可）。
> 4. 护栏两点：未表态。参谋长曾据早前授权先行落地（`6b681e1`），经指挥官指出未经其 review，已 revert 撤回，随本方案一并待审。
> 关系：BCR-015（L1 阶段内自动化）的下一级；L1 真源已全部落地（U1-U3），本方案在其机制（门禁 `l1-gates`、fail-closed、留痕对偶）之上加层，不推倒重来。

## 1. 目标形态（指挥官定义的规范化转译）

指挥官原始表述（2026-08-08）：确认 PRD（含界面要点）之后，架构详细设计 → 前后端开发 → 自测 → 部署上线全程自动；指挥官只做上线后的 UI 与功能验证，验证通过即收尾，迭代完成。第一版 PRD 由指挥官直接与 PM 确认；「多 PM 相互讨论确认」留待后续。

规范化为两个人工介入点：

| 介入点 | 时机 | 内容 | 对应决策权 |
|--------|------|------|-----------|
| ① 迭代起点 | PRD 阶段 | 指挥官与 PM 确认需求与界面要点，PRD 定稿 | A1（立项/目标）+ A2（范围定性） |
| ② 迭代终点 | 生产上线后 | 指挥官线上验证 UI 与功能；通过→自动收尾归档；打回→自动修复重发再验 | A3（验收否决权） |

两点之间（设计 → 实现 → 自测 → 部署 → 上线）**零指挥官停等**。

## 2. 与既定决策权框架（BCR-015 A 组）对照

| A 组 | 本方案处置 |
|------|-----------|
| A1 目标设定与迭代立项 | **保留** = 介入点① |
| A2 重大变更定性与裁决 | **保留**，转为中段升级路径：自动化过程中凡触及「改变什么算做好」（需求歧义无法自决、要改范围/放宽验收、架构方案推翻 PRD 前提）→ 停等回指挥官，不硬猜 |
| A3 验收否决权 | **保留** = 介入点②；打回沿用 G5 闸（打回期间禁止收尾与推进） |
| A4 对外不可逆动作放行 | **唯一需要修订的一项**，见 §3 |

结论：指挥官的定义与自己 2026-07-25 拍定的判据高度吻合，**宪法层只动 A4 的一个子项**。

## 3. A4 修订案（本方案唯一宪法级改动）

A4 现含四类：生产发布、删除受保护路径、force push、跨仓写入。修订**仅针对「生产发布」子项**，其余三类一字不动、永久停等。

**修订案：生产发布按「可逆性」切开——**

- **可自动发布**：纯代码变更（前后端代码、配置中可一键回滚项），前提三条全绿：`l1-gates` 门禁绿 + 部署就绪检查通过（含回滚预案验证）+ 本次变更不含下述任一不可逆项。
- **仍停等指挥官明示放行**：数据库 migration / 数据回填或删除 / 对外契约变更（coordination `contracts/`）/ 域名、证书、基础设施变更 / 首次上线（新项目第一次生产部署）。

理由：指挥官的验证点在上线后，生产环境成为第一验证现场——代码坏了可一键回滚（风险有界），数据和契约坏了回不去（风险非对称，维持 A4 原判据「失手代价无限」）。

## 4. 中段全自动 ≠ 裸跑（保留的机器侧结构)

1. **多角色独立互审保留**：设计/实现 Review 照旧，只是审的双方都是机器会话、不等指挥官；其可信性依赖 BCR-014 防自审护栏（产出方会话禁切 Review 角色）——故 BCR-014/016 为本方案**前置地基，同批落地**（方案已出：`docs/progress/ad-hoc/2026-08-08-bcr-014-016-guardrails-eval.md`）。
2. **门禁 fail-closed 照旧**：任何门禁非绿/查不到 → 不推进，退回指挥官 + 降级留痕。
3. **自动化中断条件（触发即停等，白名单式）**：a) 触及 A2（见 §2）；b) 触发 §3 不可逆清单；c) 门禁非绿；d) 外部依赖不可用/凭据缺失且无法降级；e) 单迭代资源预算超限（阈值默认「超 PRD 预估 3 倍」，可议）。
4. **留痕对偶照旧**：每砍一处停等，对应阶段执行记录加厚，指挥官可事后全程回溯。

## 5. 范围裁定：本次只建 L2，L3 挂起

指挥官定义的全自动完整落在**单项目单迭代**内 = 原梯度 L2（整迭代自动）+ A4 部分修订。L3（跨项目自动协调、多 PM 互审确认 PRD）当前无现实场景（存量项目全线暂停、新项目单体），**本次不建**——避免造空转机制；待新项目产生真实跨项目需求时另立 BCR。

## 6. 验证策略（指挥官已拍，2026-08-08）

1. **回放打磨**：用三个刚归档的真实迭代（xiaobao v0.6.1 / ai v0.2 / workboard v0.3）注入回放，验证 L2 规则改写后停等砍点无误砍、门禁网无逃逸（沿用 `L1_REPLAY` 机制扩展）。
2. **合并试运行**：新项目首个迭代作为 **L1+L2 合并试运行**载体，fail-closed 兜底；BCR-015 原晋级三关并入本轮评审（残留停等阈值以回放数据换算，报指挥官拍板）。

## 7. 推进节奏

1. 本方案过目通过 → coordination 登记 BCR-017（指挥官发起·参谋长代拟）。
2. BCR-014/016 护栏先行落地回流（地基）。
3. L2 规则改写（baseline 停等条文逐条过 0.2 式台账定性）→ 独立会话对抗评审（沿 BCR-015 R 轮拍子）→ 落地合 main → 回放验证 → 回流。
4. 指挥官提供立项要素 → 新项目建壳（Bootstrap 即带全量护栏 + L1+L2）→ 首迭代合并试运行 → 晋级评审。

## 8. 待指挥官拍板点（集中）

1. **A4 修订案（§3）**：生产发布按可逆性切——可回滚代码发布自动、不可逆清单停等。认可否？不可逆清单有无增删？
2. **中断条件白名单（§4.3）**：五条认可否？预算护栏阈值（默认超预估 3 倍停等）要不要、定多少？
3. **L3 挂起（§5）**：认可「本次只建 L2、多 PM 协作等有真实场景再立」？
4. **BCR-014/016 两拍点**（随本方案一并答）：① BCR-014 防自审用「入口条文级护栏」为本轮终态（CI 级会话指纹核验暂不做）；② BCR-016 禁裸 `git add -A` 但保留「批量暂存例外通道」（判据：`--cached --stat` 文件清单逐一匹配）。

---

## Review 记录

### R1 · 参谋长会话（对抗式），2026-08-08

**结论：方向认可，但须修订后再进 BCR-017。**1 项阻塞、3 项必补、1 项结构性权衡须指挥官知情。

**认可项（不必改）**：

- **§3 A4 按「可逆性」切开 —— 判据正确，且不是新造标准**。它与 BCR-015 §三之二 给 A4 的原理由（「外部世界不可回滚，风险非对称——自动化收益有限、失手代价无限」）一脉相承：代码可一键回滚 → 风险有界 → 可自动；数据 / 契约回不去 → 维持原判据。**这是把既有判据用对了地方，不是放宽它。**
- **§5 L3 挂起** —— 理由充分（无现实场景、避免造空转机制），与「一次 BCR 只锁一级」一致。
- **§2 的对照结论**（指挥官的全自动定义与其自己 2026-07-25 拍定的判据高度吻合、宪法层只动 A4 一个子项）—— 逐条核对成立，不是修辞。

**依据**：`docs/baseline/role-devops.md` / `docs/templates/prd.md` / `role-developer-detail.md` 逐条核对；`ci/gates/l1-gates.sh` 跨平台实测（详见 `docs/progress/ad-hoc/2026-08-08-bcr-014-016-guardrails-eval.md` Review 记录 B1）。

| # | 位置 | 问题 | 严重度 | 建议 |
|---|---|---|---|---|
| B1 | §1 介入点② + §4.2 | **打回之后生产环境怎么办，方案没有条款**。§1 写「打回 → 自动修复重发再验」——但打回意味着**线上正跑着坏东西**；§4.2 的 G5 闸只管「禁止推进与收尾」，**管不了已上线的生产状态**。结果是：指挥官打回 → 闸门锁住流程 → 坏版本继续在生产上跑（`news.huiyiyou.cloud` / `workboard.huiyiyou.cloud`），直到修复重发完成。且 `role-devops.md:42`「健康检查失败或关键功能不可用 → 立即回滚」**覆盖不到这一路**——那条走的是机器健康检查，而介入点②是**人工功能验证**发现的不符。**这是自动发布打开后新出现的缺口，L1 时代不存在（那时发布本身就停等）** | **阻塞** | 补一条介入点②的生产状态处置：打回 → **先自动回滚生产到上一版本** → 再进修复流程；或明确写「打回时指挥官可选回滚 / 带病修复」并把选择权显式留给指挥官。**不能默认『坏版本留在线上等修复』** |
| G1 | §3 自动发布前提第 2 条 | **「可一键回滚」是承重假设，但从未被验证过**。§3 把「部署就绪检查通过（含**回滚预案验证**）」列为自动发布三前提之一，然而真源里回滚只是**清单勾选项**（`role-devops.md:38`「是否有可回滚的上一版本，回滚步骤是否明确」），且 `:47` 把「没有验证回滚路径就直接部署」列为**常见错误**——说明这件事历史上就没真做过。人工发布时这个假设有人兜底；自动发布把它变成了**唯一的风险边界** | 高 | 首次自动发布前，对该部署通道做一次**真实回滚演练**并留痕；**演练通过的通道才允许自动发布**。未演练通道维持停等。这条同时是 B1 的前提——B1 的自动回滚也依赖回滚真的可用 |
| G2 | §4.3 中断条件 | **五条全是「事件触发」，缺「活性（liveness）」这一类**——agent 在某阶段反复重试、原地打转时：门禁没红、没触及 A2、依赖可用、预算未超 3 倍，**五条一条都不响**，全自动会静默空转 | 高 | 补第 6 条**无进展超时**（某阶段超过 N 分钟无状态推进，或同一阶段重试超 R 轮 → 停等）。框架内有现成先例可直接放大：`role-developer-detail.md:15` 子 Agent 调度已写「长时间无响应（建议上限 10 分钟）终止并由 Developer 接手」 |
| G3 | §4.3(e) 预算阈值 | **引用了不存在的字段**。阈值写「超 **PRD 预估** 3 倍」，但 `docs/templates/prd.md` 中**无任何预估 / 预算 / 资源 / 工时字段**（已全文核对）。该阈值当前无从计算，等于这条中断条件不可执行 | 高 | 二选一：① 给 PRD 模板加预估字段（属 U1 级模板改动，会扩大本轮范围）；② **本轮改用不依赖 PRD 的绝对阈值**（单迭代 token / 墙钟时长上限）。**建议②**——不为一个护栏去改 PRD 模板，与「护栏优先、不铺条文」取向一致 |
| M1 | §6.2 合并试运行 | **「L1+L2 合并试运行」违反本程序自己定的两条规矩**：整体走法「L1 晋级评审是进入 L2 的唯一入口」、元规则 2「一次 BCR 只锁一级，方向错了损失止于一级」。§6.2 把「BCR-015 原晋级三关并入本轮评审」= 两级风险叠加，**失败时无法归因到级**——而分级止损正是本程序设计里最好的那个决策。**但现实约束成立**：存量项目全线暂停，L1 无单独试运行载体，合并是务实选择而非错误 | 中 | 合并可行，但须**分层归因**：试运行时每一处被砍的停等标注属 L1 还是 L2、每一次残留停等归属到级，失败时能定位是哪一级机制不行。并在 §6.2 明写「本次为例外，理由是无单独载体」，**避免被后续会话当作『可以跨级合并』的先例** |
| M2 | 全文前置 | **L1 门禁网在其实际运行平台上有洞，本方案的机器侧地基因此不可信**。`ci/gates/l1-gates.sh:16` 的 `after_colon` 按字节偏移跳全角冒号，GNU awk（CI Linux）字符语义下取值截断 → **G5 打回闸静默 fail-open**；`c0dc32a`「试运行放行条件满足」不成立。本方案 §4.2「门禁 fail-closed 照旧」、§6.1 回放验证均建立在该门禁之上 | 中 | CI 修复列为本方案**硬前置**（与 BCR-014/016 落地同一前置）。详见 `docs/progress/ad-hoc/2026-08-08-bcr-014-016-guardrails-eval.md` Review 记录 B1 |

**§8 拍板点答复**：

1. **A4 修订案** — **认可**（判据正确，见认可项）。不可逆清单**建议增补一项**：`.github/workflows` 与门禁脚本自身的变更（改门禁 = 改判卷人，属自我授权类改动，应停等）。其余五类无异议。
2. **中断条件白名单** — 五条**认可**，但**必须补第 6 条活性条件**（G2）；预算护栏**要留**，阈值按 G3 改绝对值，具体数值待首轮回放数据出来后定，不必现在硬拍。
3. **L3 挂起** — **认可**，理由充分。
4. **BCR-014/016 两拍点** — 见 `docs/progress/ad-hoc/2026-08-08-bcr-014-016-guardrails-eval.md` Review 记录 §拍板点答复：① 认可入口条文层为本轮终态；② **建议一刀切全禁 `-A`，不保留例外通道**（与参谋长建议相反，理由见该文 G1）。

**推进顺序意见**（对 §7 的修正）：CI 修复（M2/B1）→ BCR-014/016 落地回流 → 本方案按 B1/G1/G2/G3 修订出 v2 → 登记 BCR-017。**B1 未补不宜登记**：自动发布是本方案唯一的宪法级改动，而打回后生产状态无处置正是它最危险的敞口。

### R1·B1 裁定（指挥官 2026-08-08 授权评审方确定，起草方据此出 v2）

> **授权留痕**：指挥官 2026-08-08 就 R1 阻塞项 B1（打回后生产环境处置）指示「你来确定怎么弄」，由评审会话裁定。本裁定**属规则设计、不代行 A3/A4**（A3 打回权、A4 放行权仍在指挥官）；起草方落 v2 后仍受指挥官正式 review 约束。

**裁定：打回 = 默认自动回滚生产到上一版本；指挥官可显式选择不回滚。**

判据来自 A3 自身的语义，不是新造标准：**「打回」的含义就是「这个版本不被接受」**。一个已被否决的版本没有理由继续在生产上跑——让它留着，等于把 A3 的否决权降格成「记一笔待办」。

**具体条款（三条一组，缺一不可）**：

1. **默认动作**：介入点② 指挥官打回 → **自动回滚生产至上一已验证版本** → 回滚结果写入阶段执行记录 → 再进修复流程。G5 闸照旧锁住阶段推进与收尾。
2. **例外出口**：指挥官可在打回时显式声明「**不回滚，前向修复**」（如问题轻微、回滚代价更大）。该选择**须记录理由**，进异常升级记录。**默认是回滚，前向修复是显式 opt-in**——不能反过来。
3. **回滚失败即 fail-closed**：回滚动作本身失败 → **立即升级指挥官 + 停止一切自动推进**，不重试、不前向修复、不静默。这是全案唯一「自动化动作失败」的兜底路径，必须与门禁 fail-closed 同级。

**为什么默认选回滚而不是「每次问指挥官」**：介入点② 指挥官本来就在场（正在做线上验证），加一个选择题不增加唤醒成本——但**默认值决定了指挥官思考期间线上跑的是什么**。默认回滚 = 思考期间线上是已知好版本；默认不回滚 = 思考期间线上是已知坏版本。按既有的「存疑向上取整 / fail-closed」取向，只能选前者。

**依赖关系（起草方落 v2 时须一并处理）**：

- 本裁定依赖 R1·G1（回滚演练）——**回滚没被真实验证过，本裁定就是空头承诺**。故：**未通过回滚演练的部署通道，不得开启自动发布**；该前置比本裁定优先级更高。
- 本裁定与 §3 A4 修订自洽：自动发布的范围已排除不可逆项（DB migration / 数据回填 / 契约 / 基础设施 / 首次上线），**故凡走自动发布的版本，按定义都是可回滚的**；不可逆项本就停等指挥官明示放行，不进本裁定射程。
- 首次上线（无上一版本可回滚）已在 §3 不可逆清单内、不走自动发布，无冲突。

### 指挥官决策变更（2026-08-08 晚）：取消新项目立项，改为「框架迭代全部完成后实施到存量下游」

> **原文**：「取消项目立项。完成所有的工作流迭代过后，把这个实施到下游工作项目中。」
> 本节由评审会话记录，**影响本方案 §5 / §6 / §7 三处**，起草方出 v2 时须一并修订。coordination BCR-015 条目已同步撤销原「首试点改新立项目」的改拍留痕（`8c73164`）。

**决策要点**：不再新立项目做沙盒；L1+L2 框架改造**全部完成**后，再铺到存量三仓（xiaobao / ai / workboard）。

**对本方案的三处连带修订**：

| 处 | 现文 | 应改为 |
|---|---|---|
| **§6.2 合并试运行** | 「新项目首个迭代作为 L1+L2 合并试运行载体」 | **载体回到 workboard**（13 天/迭代节奏最稳、已有测试基建与 `l1-gates`、解析侧度量同仓）；后续铺 ai、xiaobao（xiaobao 仍须先补测试基建，属 L1 范围外前置） |
| **§7.4 推进节奏** | 「指挥官提供立项要素 → 新项目建壳（Bootstrap 即带全量护栏 + L1+L2）→ 首迭代合并试运行 → 晋级评审」 | 删去立项与建壳；改为「**框架侧 L2 改造全部完成并回流 → workboard 下一真实迭代跑合并试运行 → 晋级评审 → 铺其余两仓**」 |
| **§5 L3 挂起的理由** | 「当前无现实场景（**存量项目全线暂停**、新项目单体）」 | **理由需重述**：存量三仓将在框架改造完成后重启迭代，「全线暂停」这条论据届时不再成立；但 L3 挂起的**结论仍可维持**——改用「三仓当前无在途跨项目需求需要自动流转（REQ-003 已转入 ai v0.2 内部迭代、REQ-004 属工具增强），无真实场景」为据。**结论不变、依据要换**，否则 v2 会留一条已失效的论证 |

**一项净简化（务必写进 v2，别漏掉）**：原改拍因「新项目无 L0 停等基线」而挂起了「晋级判据调整方案由参谋长另出」这项待办。**取消立项后该问题自动消失**——workboard 的 L0 基线（S1-S4 ≈7 次/迭代、残留 ≤2）已经 Owner 2026-07-26 拍板认可，晋级判据直接沿用 0.2 台账原口径，**不需要另出方案，该待办已取消**。

**评审方对本次变更的意见（一条，供指挥官知情，不构成阻挡）**：

「框架迭代**全部完成**后再实施」意味着 **L1 与 L2 的验证都被推迟到同一个时点**，且落在**真实业务仓**上——原设计里「L1 先试运行、晋级后才建 L2」的分级止损（元规则 2）在时间轴上被完全压平。这一点在 R1·M1 已经提过（当时是因为新项目是唯一载体），现在诱因变了但**后果相同且更重**：失败时不仅分不清是哪一级的机制问题，出问题的还是承载真实数据的业务仓（xiaobao 是主产品、已部署 `news.huiyiyou.cloud`）。

**缓解建议（不改变指挥官的决策，只是把风险摊平）**：

1. **回流分批，别三仓齐上**：workboard 先行跑通一个完整迭代再铺 ai、xiaobao——这不违背「框架迭代全部完成后实施」，只是实施顺序上留一道观察窗。
2. **分层归因照旧**（R1·M1）：试运行时每处被砍的停等标注属 L1 还是 L2，失败可定位到级。
3. **首个实施迭代不选高风险迭代**：避开含 DB migration / 对外契约变更的迭代（它们本就在 A4 不可逆清单里停等，但整迭代自动化首跑仍宜挑简单的）。

#### ⚠️ 上节作废：指挥官 2026-08-08 第三次改拍——仍走新立项目

> 原文：「还是立项」。**上一节「取消新项目立项」整节作废**，§5 / §6 / §7 **不必按上节修订，维持 v1 原文**（原文本就以新项目为试运行载体）。coordination BCR-015 已同步收敛为「决策沿革」四步一行、以第四步为准（`e14fe3e`）。

**现行有效（第四次 = 最终）**：L1（及后续 L2 合并）试运行首试点 = **新立项目**，自 Bootstrap 起即带 `l1-gates` + L1 免确认 + BCR-014/016 护栏全量；workboard 降为后续试点，ai / xiaobao 再次。

**遗留问题已由评审方定案，不再挂待办**：

新项目**没有 L0 停等基线**，故晋级「数据关」不能套用需要基线的相对指标（`确认类下降 ≥70%`）。**改用绝对阈值：残留确认 ≤2 次/迭代**。

依据：0.2 台账当时给出的是**一对判据**——相对指标 `下降 ≥70%` 与绝对阈值 `残留 ≤2`，且**绝对阈值已随台账经 Owner 2026-07-26 拍板认可**。相对指标只是为了抗基线误差而设的双保险；载体换成无基线的新项目后，**双保险里恰好有一条本来就不依赖基线**，直接单用即可，不需要新决策、也不需要另出调整方案。**原「晋级判据调整方案由参谋长另出」的待办据此销项。**

起草方出 v2 时把这条写进 §6.2 即可，其余保持 v1。

**评审方对「反复改拍」这件事本身的一条提醒（不阻挡，只求留痕可用）**：本决策同日翻转三次（原定 workboard → 新项目 → 取消回存量 → 仍走新项目）。文档侧已做收敛处理（coordination 用「决策沿革」一行承载全过程、本节直接标注上节作废），**不留删除线套删除线的堆叠**。但请注意一个实际后果：**L2 v2 的起草方若在两次改拍之间已按上一版动手改稿，会做无用功**——建议 v2 起草前先读本节确认现行有效版本，再动笔。
